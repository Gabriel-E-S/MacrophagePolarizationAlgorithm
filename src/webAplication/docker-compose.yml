version: '3.8'

services:
  backend:
    build: ./backend
    container_name: api-r-biologicas

  frontend:
    build: ./frontend
    container_name: apache-biologicas
    ports:
      - "80:80" 
    depends_on:
      - backend